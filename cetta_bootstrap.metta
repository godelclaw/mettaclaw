;; Establish the foreign-module environment before loading the explicit
;; equation manifest used by the CeTTa qualification mode.
!(import! &self (library lib_import))
!(import! &self ./lib_mettaclaw)
